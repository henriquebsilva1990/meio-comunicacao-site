name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Fontes internas ficam versionadas no repo, mas nao vao para a CDN publica.
      - name: Remove fontes internas do artefato de deploy
        run: rm -rf quiz-instagram/_infra

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: 1ecb790e0ce9f6e8c32caea782e59d9c
          command: pages deploy . --project-name=meio-comunicacao-site --branch=main
